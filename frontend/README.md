# GrinDorms Frontend

This folder contains the NiceGUI-based frontend for the GrinDorms project.

## Installation

Make sure you have **Python 3** installed. Then, install dependencies:

```sh
pip install nicegui

## Running the Frontend
To start the frontend, run the following command inside the frontend/ directory:
python app.py




